- insert:
    - id: smart-subagent
      name: smart-subagent
      config:
        bindingsDir: !!js process.env.SMART_SUBAGENT_BINDINGS_DIR || process.env.DSH_AGENT_BINDINGS_DIR || 'agents'
        provider: spawn
        toolName: smart_subagent
        maxDepth: 3
